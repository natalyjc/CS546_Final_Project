<div class="form-container">
  <h2 class="form-title">Create New Course</h2>
  <form action="/dashboard/courses" method="POST">
    {{#if error}}
      <div class="error-message">{{error}}</div>
    {{/if}}

    <label for="title">Course Title:</label>
    <input type="text" id="title" name="title" required />

    <label for="startDate">Start Date:</label>
  <input type="date" id="startDate" name="startDate" required min="{{todayDate}}" />

  <label for="endDate">End Date:</label>
  <input type="date" id="endDate" name="endDate" required min="{{todayDate}}" />

    <button class="btn-primary" type="submit">Create Course</button>
  </form>
</div>